Dashboard + data-layer upgrades: SSL-proof fetch, drawdown events, FIRE rewrite

Data collection
- fetch_prices.py: replace yfinance with direct Yahoo v8 REST API so
  SSL-intercepted networks (corporate VPN, custom root CA) stop failing
  with NoneType errors. Uses verify=False + urllib3 warning silence.
- fetch_prices.py: 4:1 manual split adjustment for 0050.TW on 2014-01-02
  (Taiwan zero-share split not recorded by Yahoo).
- screening.py: hardcode crypto top-5 (BTC/ETH/XRP/BNB/SOL) - stablecoins,
  wrapped variants, and exchange-specific tokens excluded.
- screening.py: add 00646.TW + 00955.TWO (TPEx .TWO suffix) to TW list.
- screening.py: YAHOO_UNAVAILABLE_TICKERS blacklist for tickers the
  upstream rankings list but Yahoo can't price.

Backtest FX
- backtest.py: TWD=X uses period1/period2 instead of range=max to avoid
  silent monthly downsampling + implausible-rate outlier filter.

Drawdown analysis
- New src/processing/drawdown_events.py: Top-N independent episodes with
  peak/trough/recovery dates, duration, curated historical-event labels
  (GFC, COVID, 2022-Bear, AI-Scare, ...).
- Fix: match_event_label treats pd.NaT the same as None (DataFrame.apply
  coerces None->NaT; old check let NaT poison every comparison and made
  ongoing episodes never match any event).

Dashboard
- Equity + drawdown charts: shaded bands for Top-5 drawdowns with #1-#5
  labels. Top-5 table is a collapsible expander.
- Correlation Check below watchlist: 3-year daily-return correlation,
  highest-pair advisory (rho >= 0.85), collapsible heatmap.
- FIRE Calculator: replace direct Retirement Target input with
  Annual Expenses / Withdrawal Rate; target is now read-only. Sibling
  "About the 4% Rule (Trinity Study)" expander documents the origin.

README
- Rewrite Features sections 1, 3, 4 to reflect: correlation check below
  watchlist, Top 5 drawdown episodes + historical event labels + shaded
  bands on equity/drawdown charts, FIRE Calculator using expenses/rate
  framing with the 4% rule expander.
- Tech Stack + Data Sources: drop yfinance, document direct Yahoo v8
  chart REST with verify=False rationale, add TradingView row, document
  hardcoded crypto top-5 and YAHOO_UNAVAILABLE_TICKERS blacklist.
- Project Structure: add drawdown_events.py, bump line count to 1530,
  update fetch_prices.py comment.
- Methodology Notes: new "Drawdown episodes" paragraph; update Backtest
  paragraph (period1/period2 + outlier filter); rewrite FIRE projection
  paragraph (target = expenses / rate); new "Risk tiers and thresholds"
  subsection with the four annualized-vol bands.
- Known Limitations: add bullets for Trinity Study extrapolation
  caveats, TradingView scraper fragility, verify=False rationale, and
  the editorial (not automatic) nature of MARKET_EVENTS labels.
